web:
  image: pdxjohnny/numapp
  command: /app/run http --cert=
  ports:
   - "12345:8080"
  volumes:
   - ./static:/app/static
  links:
   - db
db:
  image: pdxjohnny/numapp
  command: /app/run db --cert=
  ports:
    # For TESTING
   - "42345:42345"
  links:
   - mongo
mongo:
  image: mongo
